imports:
  - { resource: components/installer.yml }

parameters:
  filemanager.helpers.fqdn: 'ACP3\Modules\ACP3\Filemanager\Helpers'

services:
  filemanager.helpers:
    class: %filemanager.helpers.fqdn%
    arguments:
      - '@core.environment.application_path'
