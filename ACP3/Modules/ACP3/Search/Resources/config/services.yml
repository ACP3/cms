imports:
  - { resource: components/controllers.yml }
  - { resource: components/installer.yml }
  - { resource: components/validation.yml }

parameters:
  search.helpers.fqdn: 'ACP3\Modules\ACP3\Search\Helpers'

services:
  search.helpers:
    class: %search.helpers.fqdn%
    arguments:
      - '@core.acl'
      - '@core.modules'
      - '@core.helpers.forms'
      - '@core.eventDispatcher'
